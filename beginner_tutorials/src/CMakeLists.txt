if(Boost_FOUND)
    include_directories(${Boost_INCLUDE_DIRS})
endif()
    add_executable(${PROJECT_NAME} talker.cpp)
if(Boost_FOUND)
    target_link_libraries(${PROJECT_NAME} ${Boost_LIBRARIES} pthread)
endif()
